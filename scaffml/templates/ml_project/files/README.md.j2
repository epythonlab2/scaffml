# {{ project_name }}

This is a production-ready ML project scaffold generated by ProjForge.

## Structure

- `app/data/` – datasets
- `app/models/` – trained models
- `app/pipelines/` – preprocessing & pipelines
- `app/services/` – training & inference scripts
- `app/core/` – config & logging
- `notebooks/` – Jupyter notebooks
- `tests/` – unit tests